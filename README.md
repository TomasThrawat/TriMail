# TriMail

Native Kotlin Android mail client built with Jetpack Compose and Material 3.

The app provides three independent mailbox slots that can start their connection workflow concurrently and appear in one unified inbox.

This repository currently contains the native UI/account-slot foundation. It does not automate creating external provider accounts, bypass provider checks, or store mailbox passwords in plain-text preferences.
